const DescriptionTitle = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="w-full py-5 mx-auto text-2xl text-center">{children}</div>
  );
};

export default DescriptionTitle;
